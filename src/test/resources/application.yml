spring:
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://dev-emt6hr4gnfqxes35.us.auth0.com/.well-known/jwks.json

eureka:
  client:
    enabled: false

management:
  tracing:
    sampling:
      probability: 0
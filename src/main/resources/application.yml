spring:
  application:
    name: book-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  cloud:
    config:
      uri: ${CONFIGSERVER_URI:http://localhost:8071}
  config:
    import: "configserver:"